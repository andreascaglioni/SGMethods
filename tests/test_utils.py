import numpy as np
# from sgmethods.utils import is_downward, add_multi_index


# def test_is_downward():
#     mid_set = np.array([[0, 0], [0, 1], [1, 0], [1, 1]])
#     assert is_downward(mid_set) == True
#     mid_set_false = np.array([[0, 0], [0, 1], [1, 0], [1, 1], [2, 2]])
#     assert is_downward(mid_set_false) == False


# def test_add_multi_index():
#     mid_set = np.array([[0, 0], [0, 1], [1, 0], [1, 1]])
#     mid_set_new = add_multi_index(np.array([2, 2]), mid_set)


a=  2
a = np.reshape(a, (1,1))
print(a, a.shape)